# roboshop-ansible
we are learning Ansible through project , we have created a new repository for learning Ansible 

# To RUN 
ANSIBLE_HOST_KEY_CHECKING=False ansible-playbook -i frontend-dev.azdevopsvenkat.site, -e ansible_user=azureuser -e ansible_password=azureuser@123 -^Capp_name=frontend roboshop.yml